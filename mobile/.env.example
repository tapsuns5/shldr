# Base URL of the SHLDR web app / API this client talks to.
# Local dev: use your machine's LAN IP, not localhost, when testing on a
# physical device (the Metro bundler's localhost isn't reachable from the phone).
EXPO_PUBLIC_API_URL="http://localhost:3000"
