import { StepLabel as MuiStepLabel } from '@mui/material';
import type { StepLabelProps } from '@mui/material';

export function StepLabel(props: StepLabelProps) {
  return <MuiStepLabel {...props} />;
}
