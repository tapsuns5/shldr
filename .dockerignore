node_modules
.next
npm-debug.log
Dockerfile
.dockerignore
.env
.env*
.git
dump.rdb
.geocode-cache.json

# Prevent the marketing app from being sent to the main-app build context
marketing
marketing/**

# The mobile app and its shared packages are a separate pnpm workspace with
# their own build/deploy path; keep them out of the web app's Docker context.
mobile
mobile/**
packages
packages/**
